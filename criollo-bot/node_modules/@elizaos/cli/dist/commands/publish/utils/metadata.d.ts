import { PackageMetadata, PackageJson } from '../types';
/**
 * Generate package metadata for the registry
 */
export declare function generatePackageMetadata(packageJson: PackageJson, cliVersion: string, username: string): Promise<PackageMetadata>;
//# sourceMappingURL=metadata.d.ts.map