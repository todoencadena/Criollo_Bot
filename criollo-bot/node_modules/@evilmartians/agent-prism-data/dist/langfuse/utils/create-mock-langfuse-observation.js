/**
 * Creates a mock LangfuseObservation for testing.
 */
export function createMockLangfuseObservation(options = {}) {
    const { name = "test-observation", metadata } = options;
    const nowIso = new Date().toISOString();
    return {
        id: "obs_1",
        traceId: "trace_1",
        projectId: "proj_1",
        environment: "prod",
        parentObservationId: null,
        startTime: nowIso,
        endTime: nowIso,
        name,
        metadata,
        createdAt: nowIso,
        updatedAt: nowIso,
    };
}
//# sourceMappingURL=create-mock-langfuse-observation.js.map