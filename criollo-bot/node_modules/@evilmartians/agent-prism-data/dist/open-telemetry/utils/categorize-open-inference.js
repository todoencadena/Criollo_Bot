import { OPENINFERENCE_ATTRIBUTES, OPENINFERENCE_MAPPINGS, } from "@evilmartians/agent-prism-types";
import { getOpenTelemetryAttributeValue } from "./get-open-telemetry-attribute-value.js";
export function categorizeOpenInference(span) {
    const spanKind = getOpenTelemetryAttributeValue(span, OPENINFERENCE_ATTRIBUTES.SPAN_KIND);
    if (typeof spanKind === "string") {
        const category = OPENINFERENCE_MAPPINGS[spanKind];
        if (category)
            return category;
    }
    return "unknown";
}
//# sourceMappingURL=categorize-open-inference.js.map