import type { TraceSpan } from "@evilmartians/agent-prism-types";
export declare const getTimelineData: ({ spanCard, minStart, maxEnd, }: {
    spanCard: TraceSpan;
    minStart: number;
    maxEnd: number;
}) => {
    durationMs: number;
    startPercent: number;
    widthPercent: number;
};
//# sourceMappingURL=get-timeline-data.d.ts.map